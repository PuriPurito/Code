## ArrayOptFind

Находит первый элемент массива, удовлетворяющий заданному условию.  
Если элемент, удовлетворяющий условию, не найден, возвращается undefined.

_Синтаксис:_  
      **ArrayOptFind (<array>, <qualExpr>)**

_Аргументы:_  
      _<array> (обязательный)_  
      Тип: **Массив**. Исходный массив.  
      _<qualExpr> (обязательный)_  
      Тип: **Строка**. Выражение, определяющее соответствие элемента массива заданному критерию и вычисляемое относительно каждого элемента массива. При формировании данного выражения может быть использован указатель _This_. (в который передается текущий элемент массива).

_Возвращаемое значение:_  
      Тип: **Элемент массива**. Отобранный элемент массива. Результат действия функции.

_Пример 1:_

`_var arrA = [ 1, 2, 1, 0 ];   iA = ArrayOptFind (arrA, 'This==1');   alert (iA); // выводится значение 1_`

`_iB = ArrayOptFind (arrA, 'This==3');   alert (iB); // такого значения в массиве нет; выводится значение undefined       var arrA1 = [         {x: 1},         {x: 0},         {x: 1},    ];   iA1 = ArrayOptFind (arrA1, 'x==1'); // выбирается первый элемент массива, имеющий значение 1   alert (iA1.x); // выводится значение 1_`

_Пример 2:_  
      _try {_  
            _\_query\_str = "for $elem in collaborators where contains($elem/fullname, 'ин') return $elem";  
            personArray = XQuery(\_query\_str);  
            // personArray является массивом объектов Сотрудник, отобранных запросом. Запрос отбирает всех сотрудников, в состав фамилии, имени или отчества которых входит подстрока 'ин'.  
            // Внимание - Для больших баз данных исполнение данного запроса может потребовать некоторого времени.  
            // О формировании запросов на языке XQuery - см. Статью 1 и Статью 2._            _if (ArrayCount (personArray) == 0) alert ('Объект не найден!');  
  
            A1 = ArrayOptFind ( personArray, 'sex == ' + " 'm' " ); // функция ищет первый элемент массива personArray, удовлетворяющий заданному условию sex == m (т.е. первого мужчину из массива personArray)  
            alert (A1.fullname); // возвращает ФИО сотрудника, отобранного из массива personArray  
            A2 = ArrayOptFind ( personArray, 'id == ' + '1' ); // функция ищет элемент массива, удовлетворяющий заданному условию_  
            _alert (A2); // возвращает значение undefined (в системе отсутствует сотрудник с идентификационным номером id, равным 1). В этом случае A2 не является элементом массива.  
      }  
      catch (err){   
            alert (err);  
      };_  

_Пример 3 (аналогичен примеру 1, но с использованием указателя This):_

      `_try {               _query_str = "for $elem in collaborators where contains($elem/fullname, 'ин') return $elem";               personArray = XQuery(_query_str);               // personArray является массивом объектов Сотрудник, отобранных запросом. Запрос отбирает всех сотрудников, в состав фамилии, имени или отчества которых входит подстрока 'ин'.               // Внимание - Для больших баз данных исполнение данного запроса может потребовать некоторого времени.               if (ArrayCount (personArray) == 0) alert ('Объект не найден!');_`

            `_A1 = ArrayOptFind ( personArray, 'This.sex == ' + " 'm' " ); //_` _функция ищет первый элемент массива personArray, удовлетворяющий заданному условию sex == m (т.е. первого мужчину из массива personArray)_  
            `_alert (A1.fullname); // возвращает ФИО сотрудника, отобранного из массива personArray         }         catch (err){                alert (err);         };_`

_Пример 4 (в данном примере используется выражение, включающее в себя несколько полей элемента массива):_

      `_try {               _query_str = "for $elem in collaborators where contains($elem/fullname, 'ин') return $elem";               personArray = XQuery(_query_str);               // personArray является массивом объектов Сотрудник, отобранных запросом. Запрос отбирает всех сотрудников, в состав фамилии, имени или отчества которых входит подстрока 'ин'.               // Внимание - Для больших баз данных исполнение данного запроса может потребовать некоторого времени.               if (ArrayCount (personArray) == 0) alert ('Объект не найден!');_`

            `_A1 = ArrayOptFind ( personArray, " This.fullname+' ('+(This.sex=='m'?'мужчина':'женщина')+')' == " + " 'Калинин Михаил Михайлович (мужчина)' " ); // функция ищет первый элемент массива, удовлетворяющий заданному условию               alert (A1.fullname); // возвращает ФИО сотрудника, отобранного из массива personArray         }         catch (err){                alert (err);         };_`

---

