## ArrayExtract

Выбирает значение определенного поля (атрибута) из каждого элемента массива и возвращает новый массив той же длины, содержащий выбранные значения.

_Синтаксис:_  
      **ArrayExtract (<array>, <fieldExpr>)**

_Аргументы:_  
      _<array> (обязательный)_  
      Тип: **Массив**. Исходный массив.  
      _<fieldExpr> (обязательный)_  
      Тип: **Строка**. Строка, содержащая имя поля, по которому производится отбор (в общем случае - выражение, вычисляемое относительно каждого элемента исходного массива). При формировании данного выражения может быть использован указатель _This_ (в который передается текущий элемент массива).

_Возвращаемое значение:_  
      Тип: **Массив**. Массив, содержащий выбранные значения. Результат действия функции.

_Пример 1:_  
      _try {  
            \_query\_str = "for $elem in collaborators where contains($elem/fullname, 'ин') return $elem";  
            personArray = XQuery(\_query\_str);  
            // personArray является массивом объектов Сотрудник, отобранных запросом. Запрос отбирает всех сотрудников, в состав фамилии, имени или отчества которых входит подстрока 'ин'.  
            // Внимание - Для больших баз данных исполнение данного запроса может потребовать некоторого времени.  
            // О формировании запросов на языке XQuery - см. Статью 1 и Статью 2.  
  
            if (ArrayCount (personArray) == 0) alert ('Объект не найден!');  
  
            Array1 = ArrayExtract (personArray, 'fullname'); // функция выделяет значения атрибута, указанного во втором аргументе (т.е. полное именование (ФИО) отобранных сотрудников)  
            j=1; // переменная-счетчик для ограничения количества элементов массива  
            for (i in Array1)  
            {  
                  alert(i); // возвращает элементы массива - ФИО отобранных сотрудников  
             
                  // ограничение количества элементов, которые выводятся на экран  
                  if (j == 2) // если показано 2 элемента  
                        break; // остановить цикл  
                  j=j+1;  
            }  
      }  
      catch (err){  
           alert (err);  
      };_  
  

_Пример 2 (аналогичен примеру 1, но с использованием указателя This):_       

      `_try {               _query_str = "for $elem in collaborators where contains($elem/fullname, 'ин') return $elem";               personArray = XQuery(_query_str);               // personArray является массивом объектов Сотрудник, отобранных запросом. Запрос отбирает всех сотрудников, в состав фамилии, имени или отчества которых входит подстрока 'ин'.               // Внимание - Для больших баз данных исполнение данного запроса может потребовать некоторого времени._`  
            `_if (ArrayCount (personArray) == 0) alert ('Объект не найден!');_`

            `_Array1 = ArrayExtract (personArray, 'This.fullname'); // функция выделяет значения атрибута, указанного во втором аргументе (т.е. полное именование (ФИО) отобранных сотрудников)               j=1; // переменная-счетчик для ограничения количества элементов массива               for (i in Array1)               {                     alert(i); // возвращает элементы массива - ФИО отобранных сотрудников                                   // ограничение количества элементов, которые выводятся на экран                     if (j == 2) // если показано 2 элемента                           break; // остановить цикл                     j=j+1;               }         }         catch (err){              alert (err);         };_`

  

_Пример 3 (в данном примере используется выражение, включающее в себя несколько полей элемента массива):_       

      `_try {               _query_str = "for $elem in collaborators where contains($elem/fullname, 'ин') return $elem";               personArray = XQuery(_query_str);               // personArray является массивом объектов Сотрудник, отобранных запросом. Запрос отбирает всех сотрудников, в состав фамилии, имени или отчества которых входит подстрока 'ин'.               // Внимание - Для больших баз данных исполнение данного запроса может потребовать некоторого времени._`  
            `_if (ArrayCount (personArray) == 0) alert ('Объект не найден!');_`

            `_Array1 = ArrayExtract (personArray, " This.fullname+' ('+(This.sex=='m'?'мужчина':'женщина')+')' " ); // функция выделяет значения атрибута, указанного в выражении во втором аргументе                j=1; // переменная-счетчик для ограничения количества элементов массива               for (i in Array1)               {                     alert(i); // возвращает элементы массива - ФИО отобранных сотрудников                                   // ограничение количества элементов, которые выводятся на экран                     if (j == 2) // если показано 2 элемента                           break; // остановить цикл                     j=j+1;               }         }         catch (err){              alert (err);         };_`

_Пример 4:_       

    `_arr = XQuery ('for $elem in collaborators return $elem');       || получение нового массива, содержащий поля id, login, и fullname       arrNew = ArrayExtract(arr, '({id:id.Value, login:login.Value, fullname:fullname.Value })');       alert (tools.object_to_text (arrNew, 'json'));_`

_Пример 5:_  
    `_xArrGroupCollab = XQuery ("for $elem in group_collaborators return $elem");       ArrayExtract( xArrGroupCollab, 'Int(This.collaborator_id)');        // или ArrayExtract( xArrGroupCollab, 'This.collaborator_id.Value');_`

---

