## ArrayIntersect

Возвращает массив, содержащий элементы массива 1, у которых значение ключевого поля совпадает хотя бы с одним элементом массива 2.  
Примечание - Порядок следования элементов в возвращаемом массиве не определен. Если в массиве 1 есть элементы с одинаковым значением ключевого поля, то попадание в возвращаемый массив всех таких элементов не гарантируется. Гарантированно в возвращаемый массив попадут элементы с дублирующимся ключом в количестве, в каком элементы с этим же ключом присутствуют в массиве 2.

_Синтаксис:_  
      **ArrayIntersect (<array1>, <array2>\[, <fieldExpr1>\[, <fieldExpr2>)\])**

_Аргументы:_  
      _<array1> (обязательный)_  
      Тип: **Массив**. Исходный массив 1.  
      _<array2> (обязательный)_  
      Тип: **Массив**. Исходный массив 2.  
      _<fieldExpr1> (необязательный)_  
      Тип: **Строка**. Выражение, вычисляющее значение ключа для сравнения на основании элементов массива _<array1>_. При формировании данного выражения может быть использован указатель _This_. Если аргумент не указан, используется значение самого элемента (_This_) (в который передается текущий элемент массива).  
      _<fieldExpr2> (необязательный)_  
      Тип: **Строка**. Выражение, вычисляющее значение ключа для сравнения на основании элементов массива _<array1>_. При формировании данного выражения может быть использован указатель _This_. Если аргумент не указан, используется значение самого элемента (_This_) (в который передается текущий элемент массива).  
      Примечание - В случае, если _<array1>_ и _<array2>_ являются _массивами объектов_, рекомендуется указывать аргументы _<fieldExpr1>_ и _<fieldExpr2>_.

_Возвращаемое значение:_  
      Тип: **Массив**. Результирующий массив. Результат действия функции.

_Пример 1:_  
      _try {  
            \_query\_str1 = "for $elem in collaborators where contains($elem/fullname, 'ин') return $elem";  
            personArray1 = XQuery(\_query\_str1);  
            // personArray1 является массивом объектов **Сотрудник**, отобранных запросом. Запрос отбирает всех сотрудников, в состав фамилии, имени или отчества которых входит подстрока 'ин'.  
            if (ArrayCount (personArray1) == 0) alert ('Объект personArray1 не найден!');  
  
            \_query\_str2 = "for $elem in collaborators where contains($elem/fullname, 'Мих') return $elem";  
            personArray2 = XQuery(\_query\_str2);  
            // personArray2 является массивом объектов **Сотрудник**, отобранных запросом. Запрос отбирает всех сотрудников, в состав фамилии, имени или отчества которых входит подстрока 'Мих'.  
            // Внимание - Для больших баз данных исполнение данного запроса может потребовать некоторого времени.  
            // О формировании запросов на языке XQuery - см. Статью 1 и Статью 2.  
            if (ArrayCount (personArray2) == 0) alert ('Объект personArray2 не найден!');_  
  
            _Array1 = ArrayIntersect (personArray1, personArray2, 'fullname', 'fullname'); // функция формирует массив, являющийся пересечением массивов personArray1 и personArray2 (отбираются элементы обоих массивов, у которых значение поля 'fullname' совпадает)  
            j=1; // переменная-счетчик для ограничения количества элементов массива  
            for (a in Array1)  
            {  
                  alert(a.fullname); // возвращает элементы массива - ФИО отобранных сотрудников  
             
                  // ограничение количества элементов, которые выводятся на экран  
                  if (j == 2) // если показано 2 элемента  
                        break; // остановить цикл  
                  j=j+1;_  
            _}  
      catch (err){   
            alert ('Объект A1 не найден!');  
      };_  

_Пример 2 (аналогичен примеру 1, но с использованием указателя This):_

      `_try {               _query_str1 = "for $elem in collaborators where contains($elem/fullname, 'ин') return $elem";               personArray1 = XQuery(_query_str1);               // personArray1 является массивом объектов Сотрудник, отобранных запросом. Запрос отбирает всех сотрудников, в состав фамилии, имени или отчества которых входит подстрока 'ин'.               if (ArrayCount (personArray1) == 0) alert ('Объект personArray1 не найден!');_`

            `__query_str2 = "for $elem in collaborators where contains($elem/fullname, 'Мих') return $elem";               personArray2 = XQuery(_query_str2);               // personArray2 является массивом объектов Сотрудник, отобранных запросом. Запрос отбирает всех сотрудников, в состав фамилии, имени или отчества которых входит подстрока 'Мих'.               // Внимание - Для больших баз данных исполнение данного запроса может потребовать некоторого времени.               if (ArrayCount (personArray2) == 0) alert ('Объект personArray2 не найден!');_`

            `_Array1 = ArrayIntersect (personArray1, personArray2, 'This.fullname', 'This.fullname'); // функция формирует массив, являющийся пересечением массивов personArray1 и personArray2 (отбираются элементы обоих массивов, у которых значение поля 'fullname' совпадает)               j=1; // переменная-счетчик для ограничения количества элементов массива               for (a in Array1)               {                     alert(a.fullname); // возвращает элементы массива - ФИО отобранных сотрудников                                   // ограничение количества элементов, которые выводятся на экран                     if (j == 2) // если показано 2 элемента                           break; // остановить цикл                     j=j+1;               }         }         catch (err){                alert (err);         };_`

---

