## ArraySelectDistinct

Возвращает массив уникальных значений полей элементов заданного массива.

_Синтаксис:_  
      **ArraySelectDistinct (<array>\[, <fieldExpr>\])**

_Аргументы:_  
      _<array> (обязательный)_  
      Тип: **Массив**. Исходный массив.  
      _<fieldExpr> (необязательный)_  
      Тип: **Строка**. Строка, содержащая имя поля, по которому производится отбор уникальных значений. При формировании данного выражения может быть использован указатель _This_. Если аргумент не указан, используется значение самого элемента (_This_).

_Возвращаемое значение:_  
      Тип: **Массив**. Массив, содержащий выбранные значения. Результат действия функции.

_Пример 1:_  
      _\_query\_str = "for $elem in collaborators where contains($elem/fullname, 'ин') return $elem";  
      personArray = XQuery(\_query\_str);  
      // personArray является массивом объектов Сотрудник, отобранных запросом. Запрос отбирает всех сотрудников, в состав фамилии, имени или отчества которых входит подстрока 'ин'.  
      // Внимание - Для больших баз данных исполнение данного запроса может потребовать некоторого времени.  
      // О формировании запросов на языке XQuery - см. Статью 1 и Статью 2.  
      if (ArrayCount (personArray) == 0) alert ('Объект не найден!');_  
  
      _Array1 = ArraySelectDistinct ( personArray, 'fullname' ); // функция возвращает массив уникальных значений по полю fullname, отобранный из массива personArray  
      elemsNum = ArrayCount (Array1); // количество элементов результирующего массива  
      j=1; // переменная-счетчик для ограничения количества элементов массива  
      for ( i = 0; i < elemsNum; i++ )  
      {  
            elem = Array1 \[i\];  
            alert(elem.fullname); // возвращает полные имена всех отобранных сотрудников  
             
            // ограничение количества элементов, которые выводятся на экран  
            if (j == 2) // если показано 2 элемента  
                  break; // остановить цикл  
            j=j+1;  
      }_  
  

_Пример 2 (аналогичен примеру 1, но с использованием указателя This):_

      `__query_str = "for $elem in collaborators where contains($elem/fullname, 'ин') return $elem";         personArray = XQuery(_query_str);         // personArray является массивом объектов Сотрудник, отобранных запросом. Запрос отбирает всех сотрудников, в состав фамилии, имени или отчества которых входит подстрока 'ин'.         // Внимание - Для больших баз данных исполнение данного запроса может потребовать некоторого времени.         if (ArrayCount (personArray) == 0) alert ('Объект не найден!');_`

      `_Array1 = ArraySelectDistinct ( personArray, 'This.fullname' ); // функция возвращает массив уникальных значений по полю fullname, отобранный из массива personArray         elemsNum = ArrayCount (Array1); // количество элементов результирующего массива         j=1; // переменная-счетчик для ограничения количества элементов массива         for ( i = 0; i < elemsNum; i++ )         {               elem = Array1 [i];               alert(elem.fullname); // возвращает полные имена всех отобранных сотрудников                             // ограничение количества элементов, которые выводятся на экран               if (j == 2) // если показано 2 элемента                     break; // остановить цикл               j=j+1;         }_`

  

_Пример 3 (в данном примере используется выражение, включающее в себя несколько полей элемента массива):_

      `__query_str = "for $elem in collaborators where contains($elem/fullname, 'ин') return $elem";         personArray = XQuery(_query_str);         // personArray является массивом объектов Сотрудник, отобранных запросом. Запрос отбирает всех сотрудников, в состав фамилии, имени или отчества которых входит подстрока 'ин'.         // Внимание - Для больших баз данных исполнение данного запроса может потребовать некоторого времени.         if (ArrayCount (personArray) == 0) alert ('Объект не найден!');_`

      `_Array1 = ArraySelectDistinct ( personArray, " This.fullname+' ('+(This.sex=='m'?'мужчина':'женщина')+')' " ); // функция возвращает массив уникальных значений по указанному критерию, отобранный из массива personArray         elemsNum = ArrayCount (Array1); // количество элементов результирующего массива         j=1; // переменная-счетчик для ограничения количества элементов массива         for ( i = 0; i < elemsNum; i++ )         {               elem = Array1 [i];               alert(elem.fullname); // возвращает полные имена всех отобранных сотрудников                             // ограничение количества элементов, которые выводятся на экран               if (j == 2) // если показано 2 элемента                     break; // остановить цикл               j=j+1;         }_`

---

