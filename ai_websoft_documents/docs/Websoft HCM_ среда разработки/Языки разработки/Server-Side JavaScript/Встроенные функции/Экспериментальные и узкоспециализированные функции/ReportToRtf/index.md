## ReportToRtf

Формирование отчета в формате RTF.  
Экспериментальная функция.

_Синтаксис:_  
      **ReportToRtf (<str>)**

_Аргументы:_  
      _<str> (обязательный)_  
      Тип: **Строка**. Строковое выражение, содержащее отчет.

_Возвращаемое значение:_  
      Тип: **Строка**. Строковое выражение, содержащее результирующий отчет в формате RTF. Результат действия функции.

_Пример:_  
      `_switch ( TopElem.export_type )         {               case '.rtf':                     sRep = ReportToRtf( sRep );                     break;                             default:                     sRep = ReportToHtml( sRep );                     break;         }_`

---

