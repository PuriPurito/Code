## Write

Метод объекта Response.  
Записывает содержимое строки в тело HTTP-ответа. Содержимое предварительно накапливается в буфере или сразу направляется в сеть в зависимости от значения атрибута WriteMode.  
Именно с помощью этого метода транслируется конструкция вида _<%=xxx%>_ в ASP-страницах.

_Синтаксис:_  
      **Response.Write (<str>\[, <encode>\])**

_Аргументы:_  
      _<str> (обязательный)_  
      Тип: **Строка**. Строка с данными, которые выводятся в тело HTTP-ответа.  
      _<encode> (необязательный)_  
      Тип: **Булево**. Параметр маскирования передаваемых данных (_true_ – производится маскирование данных по правилам HTML, _false_ – маскирование не производится). По умолчанию равен _false_.

_Возвращаемое значение:_  
      Записывает содержимое строки в тело HTTP-ответа. Возвращаемое значение отсутствует.

_Пример 1:_  
      `_(Для воспроизведения данного кода можно разместить html-файл test.html с приведенным ниже кодом в папку \\WebTutor\WebTutorServer\wt\web\test\ . Запуск файл выполняется из браузера с помощью URL: http://[адрес портала]/test/test.html?param1=1 (например, http://localhost:80/test/test.html?param1=1) (где test/test.html – путь к html-файлу, param1=1 – условный параметр запроса))                 <html>               <head>                     <meta http-equiv="Content-Type" content="text/html; charset=UTF-8"/>               </head>               <body>                     WRITE: <%=Response.Write(Response.OrigRequest.Url)%><br/>                     <!-- выводит на экран значение аргумента -->               </body>         </html>_`  
  
_Результат:_  

_Пример 2:_  
      `_<% Response.Write ( '<table' ); %>         <% Response.Write ( answer.name ); %>         <% Response.Write ( "Оригинальный URL: " + sUrl ); %>         <% Response.Write ( EncodeCharset ( _sResponseText,"utf-8" ) ); %>_`

---

