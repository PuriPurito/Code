## GetTestObjects

Метод выборки коллекции объектов - получение списка объектов теста.

_Синтаксис (вызов из JS-кода):_  
      **tools.call\_code\_library\_method ("libTest", "GetTestObjects", <arrParams>)**

_Аргументы:_  
     _<arrParams> (обязательный)_  
      Тип: **Массив**. Массив, элементы которого будут переданы методу в качестве параметров. Порядок следования элементов в массиве должен соответствовать порядку параметров метода. Параметры могут быть перечислены через запятую, а весь массив - заключен в квадратные скобки.   
     Массив включает в себя пять параметров:  
     _iCourseID_ \- ID теста (целое число).  
     _iCurUserID_ \- ID текущего пользователя (целое число).  
     _bSelectObjectsTypes_ \- указать типы объектов, прикрепленных к тесту, которые вернёт выборка (булево). По умолчанию – _false_.  
     _sObjectsTypes_ \- список типов объектов (строка).  
     _bCheckAccess_\- выполнить проверку прав доступа (булево). По умолчанию – _false_.

_Возвращаемое значение:_  
      Тип: **Объект Выборка**. Содержит список объектов теста, а также ряд дополнительных атрибутов:

\- _CourseObject_ \- объекты теста (массив объектов).  
     Атрибуты отдельного объекта:  
     _id_ – ID объекта (целое число);  
     _name_ – название объекта (строка);  
     _type_ – тип объекта (строка);  
     _link_ – относительная ссылка на файл (строка).

\- _result_ – результат выполнения метода (_true_ – если операция завершилась успешно, _false_ – в противном случае) (булево).  
\- _error_ \- код ошибки (при отсутствии ошибок код равен 0) (целое число).  
\- _errorText_ – сообщение об ошибке (строка).

_Пример:_

`_// Вызов метода с помощью функции_ _tools.call_code_library_method_   _oRes = tools.call_code_library_method ("libTest", "GetTestObjects", [ iTestID, iCurUserID, bSelectObjectsTypes, sObjectsTypes, bCheckAccess ]);_`

`_// Запись результата в основной журнал (лог) системы xhttp-<текущая_дата>.log   LogEvent('', EncodeJson(oRes));_`

---

