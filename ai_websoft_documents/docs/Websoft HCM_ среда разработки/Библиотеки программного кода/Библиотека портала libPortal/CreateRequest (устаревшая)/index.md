## CreateRequest (устаревшая)

Метод **CreateRequest** предназначен для создания заявки.

Примечание. Начиная с версии 2022.3, данная функция помечена как **deprecated**.   
Данную функцию использовать не рекомендуется. Вместо нее следует использовать функцию **CreateRequestMain.**  

_Синтаксис (вызов из JS-кода):_  
      **tools.call\_code\_library\_method ("libPortal", "CreateRequest", <arrParams>)**

_Аргументы:_  
     _<arrParams> (обязательный)_  
      Тип: **Массив**. Массив, элементы которого будут переданы методу в качестве параметров. Порядок следования элементов в массиве должен соответствовать порядку параметров метода. Параметры могут быть перечислены через запятую, а весь массив - заключен в квадратные скобки.   
     Массив включает в себя четыре параметра:  
     _iPersonID_ – ID сотрудника (целое число).  
     _iRequestTypeID_ – ID типа заявки (целое число).  
     _iObjectID_ – ID объекта, на основании которого формируется заявка (целое число).  
     _sFormFields_ \- список форм полей (строка).

_Возвращаемое значение:_  
      Тип:  **Объект**. Содержит следующие атрибуты:  
\- _result_ – результат вызова метода (_true_ – если операция завершилась успешно, _false_ – в противном случае) (булево).  
\- _error_ \- код ошибки (при отсутствии ошибок код равен 0) (целое число).  
\- _errorText_ – текст ошибки (строка).

_Пример:_

`_// Вызов метода с помощью функции_ _tools.call_code_library_method_   _oRes = tools.call_code_library_method ("libPortal", "CreateRequest", [iPersonID, iRequestTypeID, iObjectID, sFormFields]);_` 

`_// Запись результата в основной журнал (лог) системы xhttp-<текущая_дата>.log   LogEvent('', EncodeJson(oRes));_`

---

