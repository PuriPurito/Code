## GetAddressList

Метод GetAddressList предназначен для получения информации о сотрудниках из адресной книги портала.

_Синтаксис (вызов из JS-кода):_  
      **tools.call\_code\_library\_method ("libPortal", "GetAddressList", <arrParams>)**

_Аргументы:_  
     _<arrParams> (обязательный)_  
      Тип: **Массив**. Массив, элементы которого будут переданы методу в качестве параметров. Порядок следования элементов в массиве должен соответствовать порядку параметров метода. Параметры могут быть перечислены через запятую, а весь массив - заключен в квадратные скобки.   
     Массив включает в себя восемь параметров:  
     _cLetter_ \- буква с которой начинается ФИО сотрудника (строка).  
     _sFIO_ – ФИО сотрудника (строка).  
     _sPosition_ \- наименование должности для поиска (строка).  
     _sSubdivision_ \- наименование подразделения для поиска (строка).  
     _sLimitationType_ – тип ограничения (строка). Допустимые значения: _'org'_ - в одной организации, _'subdivision'_ - в одном подразделении.   
     _iPersonID_ – ID текущего сотрудника (целое число).   
     _oAttribute_ – параметры для фильтрации списка сотрудников (объект).  
     _oCollectionParams_ \- Набор интерактивных параметров (отбор, сортировка, пейджинг) (объект).

_Возвращаемое значение:_  
      Тип: **Объект**. Содержит перечень информации о сотрудниках из адресной книги портала, а также ряд дополнительных атрибутов:  
\- _result_ \- массив информации о сотрудниках (массив объектов).  
     Атрибуты отдельного объекта:  
     _id_ \- ID сотрудника (целое число);  
     _name_ – ФИО сотрудника (строка);  
     _position\_id_ \- ID должности (целое число);  
     _position\_name_ \- название должности (строка);  
     _org\_id_ \- ID организации (целое число);  
     _org\_name_ \- название организации (строка);  
     _subdivision\_id_ \- ID подразделения (целое число);  
     _subdivision\_name_ \- название подразделения (строка);  
     _birth\_date_ \- дата рождения (дата);  
     _birth\_date\_name_ \- дата рождения в строчном формате (строка);  
     _birth\_date\_in\_cur\_year_ \- дата рождения в текущем году (дата);  
     _birth\_date\_rest\_days\_num_ \- число дней до дня рождения (целое число);  
     _email_ \- адрес электронной почты (строка);  
     _phone_ \- телефон (строка);  
     _message_ \- дополнительная (служебная) информация о сотруднике (строка);  
     _image\_url_ \- URL изображения сотрудника (строка);  
     _form\_url_ \- ссылка на карточку сотрудника (строка).

\- _error_ \- код ошибки (при отсутствии ошибок код равен 0) (целое число).  
\- _errorText_ – текст ошибки (строка).

_Пример:_

`_// Вызов метода с помощью функции_ _tools.call_code_library_method_   _oRes = tools.call_code_library_method ("libPortal", "GetAddressList", [ cLetter, sFIO, sPosition, sSubdivision, sLimitationType, iPersonID, oAttribute, oCollectionParams]);_` 

`_// Запись результата в основной журнал (лог) системы xhttp-<текущая_дата>.log   LogEvent('', EncodeJson(oRes));_`

---

