## GetCareerReserveTypeContext

Метод **GetCareerReserveTypeContext** предназначен для вычисления контекста для типа кадрового резерва.

_Синтаксис (вызов из JS-кода):_  
      **tools.call\_code\_library\_method ("libTalentPool", "GetCareerReserveTypeContext", <arrParams>)**

_Аргументы:_  
     _<arrParams> (обязательный)_  
      Тип: **Массив**. Массив, элементы которого передаются методу в качестве параметров. Порядок следования элементов в массиве должен соответствовать порядку параметров метода.  
     Массив включает в себя один параметр:  
     _iCareerReserveTypeIDParam_ \- ID типа кадрового резерва (целое число).

_Возвращаемое значение:_  
      Тип: **Объект**. Содержит информацию о типе кадрового резерва, а также ряд дополнительных атрибутов:  
\- _context_ \- информация о типе кадрового резерва (объект).  
     Атрибуты объекта:  
     _in\_reserve\_count_ – количество резервистов этого типа со статусом «Состоит в резерве» (целое число).  
     _candidate\_count_ – количество резервистов этого типа со статусом «Кандидат» (целое число).  
     _left\_reserve\_count_ – количество резервистов этого типа со статусом «Исключен из резерва» (целое число).  
     _failed\_count_ – количество резервистов этого типа со статусом «Не прошел испытания» (целое число).

\- _error_ \- код ошибки (при отсутствии ошибок код равен 0) (целое число).  
\- _errorText_ – текст ошибки (строка).

_Пример:_

`_// Вызов метода с помощью функции_ _tools.call_code_library_method_   _oRes = tools.call_code_library_method ("libTalentPool", "GetCareerReserveTypeContext", [iCareerReserveTypeIDParam ]);_` 

`_// Запись результата в основной журнал (лог) системы xhttp-<текущая_дата>.log   LogEvent('', EncodeJson(oRes));_`

---

