## GetBossLearningsStatByPerson

Метод **GetBossLearningsStatByPerson** предназначен для получения информации по обучению (прохождению курсов/тестов) с агрегацией по сотрудникам.

_Синтаксис (вызов из JS-кода):_  
      **tools.call\_code\_library\_method ("libLearning", "GetBossLearningsStatByPerson", <arrParams>)**

_Аргументы:_  
     _<arrParams> (обязательный)_  
      Тип: **Массив**. Массив, элементы которого будут переданы методу в качестве параметров. Порядок следования элементов в массиве должен соответствовать порядку параметров метода. Параметры могут быть перечислены через запятую, а весь массив - заключен в квадратные скобки.   
     Массив включает в себя три параметра:  
     _iPersonID_ \- ID сотрудника (целое число).  
     _sActivityType_ \- тип активности (строка).  
     _oCollectionParams_ \- набор интерактивных параметров выборки (объект). В состав объекта могут входить следующие параметры: paging (пейджинг, настройка страниц) (объект) (возможные параметры настройки страниц: paging.INDEX - номер страницы (целое число);  paging.SIZE - размер страницы (целое число); paging.START\_INDEX - номер начального элемента текущей страницы (окна выборки); paging.MANUAL - разделение на страницы вручную (булево); paging.TOTAL - общее количество страниц (целое число)), sort (сортировка) (объект) (sort.FIELD - название поля, по которому производится сортировка (строка), sort.DIRECTION - направление сортировки (строка) (допустимые значения:  "ASC" (сортировка по возрастанию), "DESC" (сортировка по убыванию)), distincts (дополнительная входная информация) (массив строк), filters (фильтры) (массив объектов) (возможные параметры фильтрации: filter.type - тип фильтрации (строка) (например: 'search' (поиск), filter.value - критерий фильтрации (строка), filter.id - идентификатор (имя) фильтра (строка))), fulltext - критерий выборки (строка).

_Возвращаемое значение:_  
      Тип: **Объект**. Содержит информацию по обучению (прохождению курсов/тестов) с агрегацией по сотрудникам, а также ряд дополнительных атрибутов:  
\- _array_ \- массив данных по обучению с агрегацией по сотрудникам (массив объектов).  
     Атрибуты отдельного объекта:  
     _person\_id_ \- ID сотрудника (целое число);  
     _img_ – относительная ссылка на изображение (строка);  
     _link_ – относительная ссылка на карточку сотрудника (строка);  
     _name_ – ФИО сотрудника (строка);  
     _position\_name_ – должность сотрудника (строка);  
     _compulsory\_percent_ – процент объектов обучения, назначенных пользователю администратором (целое число);  
     _success\_percent_ – процент объектов обучения, которые пользователь прошел успешно (целое число);  
     _total\_learn\_num_ – общее количество изученых объектов (целое число);  
     _self\_num_ – количество объектов обучения, которые пользователь назначил себе самостоятельно (целое число);  
     _learn\_num_ – количество незаконченных объектов обучения (целое число).

\- _error_ \- код ошибки (при отсутствии ошибок код равен 0) (целое число).  
\- _errorText_ – текст ошибки (строка).

_Пример:_

`_// Вызов метода с помощью функции_ _tools.call_code_library_method_   _oRes = tools.call_code_library_method ("libLearning", "GetBossLearningsStatByPerson", [iPersonID, sActivityType, oCollectionParams]);_` 

`_// Запись результата в основной журнал (лог) системы xhttp-<текущая_дата>.log   LogEvent('', EncodeJson(oRes));_`

---

