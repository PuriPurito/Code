## GetCommonConversations

Метод **GetCommonConversations** предназначен для получения списка общих разговоров.

_Синтаксис (вызов из JS-кода):_  
      **tools.call\_code\_library\_method ("libConversationApi", "GetCommonConversations", <arrParams>)**

_Аргументы:_  
     _<arrParams> (обязательный)_  
      Тип: **Массив**. Массив, элементы которого передаются методу в качестве параметров. Порядок следования элементов в массиве должен соответствовать порядку параметров метода.  
     Массив включает в себя один параметр:  
     _oActionParam_ \- входные параметры функции (объект). В состав объекта входят следующие параметры: conversation\_id – ID разговора (целое число). 

_Возвращаемое значение:_  
      Тип: **Объект**. Содержит перечень общих разговоров, а также ряд дополнительных атрибутов:  
\- common\_conversations - массив общих разговоров (массив объектов).  
     Атрибуты отдельного объекта:  
     _id_ – ID разговора (целое число).  
     _name_ – название разговора (строка).  
     _state\_id_ – ID статуса разговора (строка).  
     _active\_call_ – имеется ли активный звонок (булево).  
     _icon\_url_ – ссылка на изображение разговора (строка).

\- _error_ \- код ошибки (при отсутствии ошибок код равен 0) (целое число).  
\- _message_– текст ошибки (строка).

_Пример:_

`_// Вызов метода с помощью функции_ _tools.call_code_library_method_   _oRes = tools.call_code_library_method ("libConversationApi", "GetCommonConversations", [oActionParam]);_` 

`_// Запись результата в основной журнал (лог) системы xhttp-<текущая_дата>.log   LogEvent('', EncodeJson(oRes));_`

---

