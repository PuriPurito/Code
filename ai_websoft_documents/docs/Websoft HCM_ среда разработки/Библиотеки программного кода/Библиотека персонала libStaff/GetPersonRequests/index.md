## GetPersonRequests

Метод **GetPersonRequests** предназначен для получения списка заявок на обучение, которые подал сотрудник.

_Синтаксис (вызов из JS-кода):_  
      **tools.call\_code\_library\_method ("libStaff", "GetPersonRequests", <arrParams>)**

_Аргументы:_  
     _<arrParams> (обязательный)_  
      Тип: **Массив**. Массив, элементы которого будут переданы методу в качестве параметров. Порядок следования элементов в массиве должен соответствовать порядку параметров метода. Параметры могут быть перечислены через запятую, а весь массив - заключен в квадратные скобки.   
     Массив включает в себя пять параметров:  
     _iPersonID_ \- ID сотрудника (целое число).  
     _iRequestTypeID_ \- ID типа заявки (целое число).  
     _sStatus_ \- статус заявки (строка).  
     _aTypeCodes_ \- коды типа заявки (массив строк).  
     _oCollectionParams_ \- параметры выборки (объект) (параметры объекта: _filters, distincts, paging_).

_Возвращаемое значение:_  
      Тип: **Объект Выборка**. Содержит данные о заявках на обучение, которые подал сотрудник, а также ряд дополнительных атрибутов:  
\- _array_ \- массив заявок на обучение, которые подал сотрудник (массив объектов).  
     Атрибуты отдельного объекта:  
     _id_ – ID заявки (целое число);  
     _code_ – код заявки (строка);  
     _create\_date_ – дата формирования заявки (дата);  
     _request\_type\_id_ – ID типа заявки (целое число);  
     _request\_type\_name_ – название типа заявки (строка);  
     _type_ – каталог типа заявки (строка);  
     _object\_id_ – ID объекта, с которым связана заявка (целое число);  
     _object\_name_ – название объекта, с которым связана заявка (строка);  
     _status\_id_ – ID статуса заявки (строка) (например, _'active', 'close', 'ignore', 'pay'_);  
     _status\_name_ – название статуса заявки (строка) (например, _'Активная', 'Закрыта', 'Отклонена', 'Оплачена'_);  
     _workflow\_state\_id_ \- ID этапа документооборота (строка).  
     _workflow\_state\_name_ \- этап документооборота (строка).  
     _link_ – относительная ссылка на заявку (строка).

`_// Вызов метода с помощью функции_ _tools.call_code_library_method_   _oRes = tools.call_code_library_method ("libStaff", "GetPersonRequests", [ iPersonID, iRequestTypeID, sStatus, oCollectionParams, aTypeCodes ]);_`

`_// Запись результата в основной журнал (лог) системы xhttp-<текущая_дата>.log   LogEvent('', EncodeJson(oRes));_`

---

