## GetAcquaintAssigns

Метод **GetAcquaintAssigns** предназначен для получения списка назначенных ознакомлений.

_Синтаксис (вызов из JS-кода):_  
      **tools.call\_code\_library\_method ("libMain", "GetAcquaintAssigns", <arrParams>)**

_Аргументы:_  
     _<arrParams> (обязательный)_  
      Тип: **Массив**. Массив, элементы которого будут переданы методу в качестве параметров. Порядок следования элементов в массиве должен соответствовать порядку параметров метода. Параметры могут быть перечислены через запятую, а весь массив - заключен в квадратные скобки.   
     Массив включает в себя восемь параметров:  
\- _iCurUserID_ \- ID текущего пользователя (целое число).  
\- _sAccessType_ \- тип доступа (строка). Допустимые значения: _"admin"; "manager"; "hr"; "expert"; "observer"; "auto"._  
\- _sApplication_ \- код приложения, по которому определяется доступ (строка).  
\- _iCurApplicationID_ \- ID текущего приложения (целое число).  
\- _arrState_ - массив состояний (строка) (необязательный). Допустимые значения: _"assigned" (Назначен), "active" (В процессе), "familiar" (Ознакомлен)_.  
\- _sFilter_ \- строка для XQuery-фильтра (строка).  
\- _sFulltext_ \- строка для полнотекстового поиска (строка).   
\- _oCollectionParams_ \- набор интерактивных параметров выборки (отбор, сортировка, пейджинг) (объект).

_Возвращаемое значение:_  
      Тип: **Объект**. Содержит перечень назначенных ознакомлений, а также ряд дополнительных атрибутов:  
\- _array_ \- массив назначенных ознакомлений (массив объектов).  
     Атрибуты отдельного объекта:  
     _code_ \- код ознакомления (строка).  
     _fullname_ \- ФИО ознакомленного сотрудника (строка).  
     _acquaint\_name_ \- название ознакомления (строка).  
     _acquaint\_assign\_state_ \- состояние (строка).  
     _normative\_date_ \- требуемая дата ознакомления (дата).  
     _finish\_date_ \- дата ознакомления (дата).

\- _error_ \- код ошибки (при отсутствии ошибок код равен 0) (целое число).  
\- _errorText_ – текст ошибки (строка).

_Пример:_

`_// Вызов метода с помощью функции_ _tools.call_code_library_method_   _oRes = tools.call_code_library_method ("libMain", "GetAcquaintAssigns", [iCurUserID, sAccessType, sApplication, iCurApplicationID, arrState, sFilter, sFulltext, oCollectionParams]);_` 

`_// Запись результата в основной журнал (лог) системы xhttp-<текущая_дата>.log   LogEvent('', EncodeJson(oRes));_`

---

