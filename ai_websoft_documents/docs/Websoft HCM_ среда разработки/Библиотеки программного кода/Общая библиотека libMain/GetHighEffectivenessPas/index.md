## GetHighEffectivenessPas

Метод **GetHighEffectivenessPas** предназначен для получения анкет для вычисления эффективности сотрудников.

_Синтаксис (вызов из JS-кода):_  
      **tools.call\_code\_library\_method ("libMain", "GetHighEffectivenessPas", <arrParams>)**

_Аргументы:_  
     _<arrParams> (обязательный)_  
      Тип: **Массив**. Массив, элементы которого передаются методу в качестве параметров. Порядок следования элементов в массиве должен соответствовать порядку параметров метода.  
          Массив включает в себя три параметра:  
     _arrPersonsIDs_ \- массив ID сотрудников (массив целых чисел).  
     _arrAssessmentAppraiseIDs_ \- массив ID процедур оценки (массив целых чисел).  
     _bLevelIgnore_ \- не нужно ли учитывать значение уровня высокой эффективности _DefaultHighEffectivenessLevel_ из параметров библиотеки (булево). Если параметр имеет значение true, то значение уровня высокой эффективности учитывать не нужно. 

_Возвращаемое значение:_  
      Тип: **Объект**. Содержит анкеты для вычисления эффективности сотрудников, а также ряд дополнительных атрибутов:  
\- _array_ \- массив анкет пользователей для вычисления эффективности сотрудников (массив объектов).  
     Атрибуты отдельного элемента массива:  
     _person\_id_ – ID сотрудника (целое число).  
     _pa\_id_ – ID анкеты (целое число).

\- _error_ \- код ошибки (при отсутствии ошибок код равен 0) (целое число).  
\- _errorText_ – текст ошибки (строка).

_Пример:_

`_// Вызов метода с помощью функции_ _tools.call_code_library_method_   _oRes = tools.call_code_library_method ("libMain", "GetHighEffectivenessPas", [arrPersonsIDs, arrAssessmentAppraiseIDs, bLevelIgnore]);_` 

`_// Запись результата в основной журнал (лог) системы xhttp-<текущая_дата>.log   LogEvent('', EncodeJson(oRes));_`

---

