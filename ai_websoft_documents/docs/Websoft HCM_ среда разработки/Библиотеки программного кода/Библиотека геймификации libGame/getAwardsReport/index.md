## getAwardsReport

Метод **getAwardsReport** предназначен для получения отчета по наградам.

_Синтаксис (вызов из JS-кода):_  
      **tools.call\_code\_library\_method ("libGame", "getAwardsReport", <arrParams>)**

_Аргументы:_  
     _<arrParams> (обязательный)_  
      Тип: **Массив**. Массив, элементы которого передаются методу в качестве параметров. Порядок следования элементов в массиве должен соответствовать порядку параметров метода.     Массив включает в себя три параметра:  
     _Ps_ \- объект, по которому формируется отчет (необязательный) (объект).  
     _iCurUserID_ \- ID текущего пользователя (целое число).  
     _sApplicationID_ \- код приложения, по которому определяется доступ (строка).

_Возвращаемое значение:_  
      Тип: **Объект**. Содержит перечень наград, а также ряд дополнительных атрибутов:  
\- _awards_ – массив наград (массив объектов).  
     Атрибуты отдельного объекта:  
     _id_ \- (целое число).  
     _award\_name_ \- название награды (строка).  
     _person\_fullname_ \- ФИО сотрудника (строка).  
     _person\_position\_name_ \- должность сотрудника (строка).  
     _person\_subdivision\_name_ \- подразделение, в котором работает сотрудник (строка).  
     _person\_org\_name_ \- организация, в которой работает сотрудник (строка).  
     _qualif\_ass\_date_ \- дата получения квалификации (награды) (дата).  
     _sender\_name_ \- имя сотрудника, выдавшего награду (строка).  
     _status_ \- статус награды (строка).  
     _competence\_name_ \- название компетенции (строка).  
     _comment_ \- комментарий (строка).

\- _error_ \- код ошибки (при отсутствии ошибок код равен 0) (целое число).  
\- _errorText_ – текст ошибки (строка).

_Пример:_

`_// Вызов метода с помощью функции_ _tools.call_code_library_method_   _oRes = tools.call_code_library_method ("libGame", "getAwardsReport", [Ps, iCurUserID, sApplicationID]);_` 

`_// Запись результата в основной журнал (лог) системы xhttp-<текущая_дата>.log   LogEvent('', EncodeJson(oRes));_`

---

