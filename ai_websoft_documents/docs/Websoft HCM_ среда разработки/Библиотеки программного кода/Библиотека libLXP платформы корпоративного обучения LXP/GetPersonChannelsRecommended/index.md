## GetPersonChannelsRecommended

Метод **GetPersonChannelsRecommended** предназначен для получения списка рекомендованных пользователю каналов.

  Примечание. Канал - это блог, имеющий тип **"Канал"**.  

_Синтаксис (вызов из JS-кода):_  
      **tools.call\_code\_library\_method ("libLXP", "GetPersonChannelsRecommended", <arrParams>)**

_Аргументы:_  
     _<arrParams> (обязательный)_  
      Тип: **Массив**. Массив, элементы которого передаются методу в качестве параметров. Порядок следования элементов в массиве должен соответствовать порядку параметров метода. Параметры могут быть перечислены через запятую, а весь массив - заключен в квадратные скобки.   
     Массив включает в себя десять параметров:  
     _iPersonID_ \- ID сотрудника (целое число).  
     _Request_ \- объект запроса (целое число).  
     _isForPostEntrys_ \- показывать только свои каналы (булево).  
     _isIncludeAutorsChannels_ \- включать в состав свои каналы (булево).  
     oSort - информация из рантайма о сортировке (объект). Объект включает в себя следующие данные: FIELD - название поля, по которому производится сортировка (строка), DIRECTION - направление сортировки (строка) (допустимые значения:  "ASC" (сортировка по возрастанию), "DESC" (сортировка по убыванию)).  
     oPaging - информация из рантайма о пейджинге (объект). Объект включает в себя следующие данные:  INDEX - номер страницы (целое число);  SIZE - размер страницы (целое число); MANUAL - разделение на страницы вручную (булево); TOTAL - общее количество страниц (целое число).  
     _iMaxCount_ \- максимальное количество каналов в выборке (целое число).  
     _bReturnId_ \- возвращать в результатах только ID канала (булево). Значение по умолчанию - _false_.  
     _knowledgeTypeId_ \- тип знания пользователя (строка). Значение по умолчанию - _all_.  
     _bHierarchy_ \- учитывать иерархию знаний (булево). Значение по умолчанию - _false_. 

_Возвращаемое значение:_  
      Тип: **Объект**. Содержит перечень рекомендованных пользователю каналов, а также ряд дополнительных атрибутов:  
\- _channels_ \- массив каналов (массив объектов).  
     Атрибуты отдельного объекта:  
     _id_ – ID канала в текстовом формате (строка).  
     _type_ – тип канала (строка).  
     _name_ – наименование канала (строка).

\- _error_ \- код ошибки (при отсутствии ошибок код равен 0) (целое число).  
\- _errorText_ – текст ошибки (строка).

_Пример:_

`_// Вызов метода с помощью функции_ _tools.call_code_library_method_   _oRes = tools.call_code_library_method ("libLXP", "GetPersonChannelsRecommended", [iPersonID, Request, isForPostEntrys, isIncludeAutorsChannels, oSort, oPaging, iMaxCount, bReturnId, knowledgeTypeId, bHierarchy]);_` 

`_// Запись результата в основной журнал (лог) системы xhttp-<текущая_дата>.log   LogEvent('', EncodeJson(oRes));_`

---

