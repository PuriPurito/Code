## GetListTargetsLxpSubs

Метод **GetListTargetsLxpSubs** предназначен для получения списка каналов, на которые подписан пользователь, и каналов, где пользователь является автором.

  Примечание. Канал - это блог, имеющий тип **"Канал"**.  

_Синтаксис (вызов из JS-кода):_  
      **tools.call\_code\_library\_method ("libLXP", "GetListTargetsLxpSubs", <arrParams>)**

_Аргументы:_  
     _<arrParams> (обязательный)_  
      Тип: **Массив**. Массив, элементы которого передаются методу в качестве параметров. Порядок следования элементов в массиве должен соответствовать порядку параметров метода. Параметры могут быть перечислены через запятую, а весь массив - заключен в квадратные скобки.   
     Массив включает в себя четыре параметра:  
     _iPersonID_ \- ID сотрудника (целое число).  
     _sChannelType_ \- тип возвращаемых каналов (строка). Допустимые значения - "autor" (мои каналы), "subscription" (каналы по подписке), "all" (все).  
     _Request_ \- объект запроса (целое число).  
     _isForPostEntrys_ \- показывать только свои каналы (булево). 

_Возвращаемое значение:_  
      Тип: **Объект**. Содержит перечень каналов, на которые подписан пользователь, и каналов, где пользователь является автором, а также ряд дополнительных атрибутов:  
\- _array_ \- массив мероприятий (массив объектов).  
     Атрибуты отдельного объекта:  
     _id_ – ID канала в текстовом формате (строка).  
     _type_ – тип канала (строка).  
     _name_ – наименование канала (строка).

_Пример:_

`_// Вызов метода с помощью функции_ _tools.call_code_library_method_   _oRes = tools.call_code_library_method ("libLXP", "GetListTargetsLxpSubs", [iPersonID, sChannelType, Request, isForPostEntrys]);_` 

`_// Запись результата в основной журнал (лог) системы xhttp-<текущая_дата>.log   LogEvent('', EncodeJson(oRes));_`

---

