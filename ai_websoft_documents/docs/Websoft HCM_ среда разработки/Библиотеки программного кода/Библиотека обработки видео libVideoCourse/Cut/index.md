## Cut

Метод **Cut** предназначен для создания фрагмента видеофайла.

_Синтаксис (вызов из JS-кода):_  
      **tools.call\_code\_library\_method ("libVideoCourse", "Cut", <arrParams>)**

_Аргументы:_  
     _<arrParams> (обязательный)_  
      Тип: **Массив**. Массив, элементы которого передаются методу в качестве параметров. Порядок следования элементов в массиве должен соответствовать порядку параметров метода.  
     Массив включает в себя четыре параметра:  
     _sOutputPath_ \- путь для сохранения выходного файла (строка).  
     _sInputVideo_ \- путь до видеофайла, который нужно обработать (строка).  
     _sTimeStart_ \- время начала в строковом выражении (строка).  
     _sTimeEnd_ \- время окончания в строковом выражении (строка).

_Возвращаемое значение:_  
     _sOutputPath_ \- путь выходного видеофайла (строка).

_Пример:_

`_// Вызов метода с помощью функции_ _tools.call_code_library_method_   _oRes = tools.call_code_library_method ("libVideoCourse", "Cut", [sOutputPath, sInputVideo, sTimeStart, sTimeEnd]);_` 

`_// Запись результата в основной журнал (лог) системы xhttp-<текущая_дата>.log   LogEvent('', EncodeJson(oRes));_`

---

